@using TwitterBackup.Services.ViewModels
@model TwitterAccountViewModel

@{
    ViewBag.Title = "Search Result:";
}  
    <div class="row">
        <div class="col-lg-4">
            <div class="widget-head-color-box navy-bg p-lg text-center">
                <div class="m-b-md">
                    <h2 class="font-bold no-margins">
                        @Model.ScreenName
                    </h2>
                </div>
                <img src="@Model.ProfileImageUrl" class="img-circle circle-border m-b-md" alt="profile">
                <div>
                    <span>@Model.StatusesCount Tweets</span> |
                    <span>@Model.FriendsCount Friends</span> |
                    <span>@Model.FollowersCount Followers</span>
                </div>
            </div>
            <div class="widget-text-box">
                <h4 class="media-heading">@Model.Name</h4>
                <p>@Model.Description</p>
                <div class="text-right">
                    <button id="save" class="btn btn-sm btn-primary">Save</button>
                </div>
            </div>
            
        </div>
    </div>   